To start the frontend: 
npm init
npm install 
after it to run the frontend: npx vite

to start the backend:
there are different services you can start them on your own or just run "docker-compose up --build"
